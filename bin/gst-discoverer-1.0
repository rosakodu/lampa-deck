#!/bin/bash
# Run flatpak as the 'deck' user because Decky Loader runs as root, and flatpak refuses to run GUI/user apps as root
exec sudo -u deck XDG_RUNTIME_DIR=/run/user/1000 flatpak run --command=gst-discoverer-1.0 org.videolan.VLC "$@"
